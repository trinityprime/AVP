When characters/items are added, database does not *visually* update until program is quit. 
However, characters/items ARE in the database, just that it does not appear until the program is quit. 
For example, when characters are listed, they do show up.